# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: home-header-nav.test.js >> TC-002: Week Label - Default
- Location: tests/home-header-nav.test.js:27:5

# Error details

```
Error: expect(locator).toHaveText(expected) failed

Locator:  locator('#weekLabel')
Expected: "Week 12 • August 24 - August 30"
Received: "Week 4 • June 29 - July 5"
Timeout:  5000ms

Call log:
  - Expect "toHaveText" with timeout 5000ms
  - waiting for locator('#weekLabel')
    14 × locator resolved to <h2 id="weekLabel" class="subtitle">Week 4 • June 29 - July 5</h2>
       - unexpected value "Week 4 • June 29 - July 5"

```

```yaml
- heading "Week 4 • June 29 - July 5" [level=2]
```

# Test source

```ts
  1   | // @ts-check
  2   | import { test, expect } from "@playwright/test";
  3   | import { generateExpectedWeekLabel } from "../helpers/date-utils.js";
  4   | const { HomePage } = require("../pom/home.page");
  5   | 
  6   | test.beforeEach(async ({ page }) => {
  7   |   await page.goto(process.env.APP_URL);
  8   | });
  9   | 
  10  | /*******************
  11  |       HEADER
  12  | ********************/
  13  | test(
  14  |   "TC-001: Title",
  15  |   {
  16  |     tag: ["@smoke", "@header", "@TC-001"],
  17  |   },
  18  |   async ({ page }) => {
  19  |     const home = new HomePage(page);
  20  |     await expect(home.title).toHaveText(process.env.APP_TITLE);
  21  |   },
  22  | );
  23  | 
  24  | /*******************
  25  |       WEEK LABEL
  26  | ********************/
  27  | test(
  28  |   "TC-002: Week Label - Default",
  29  |   {
  30  |     tag: ["@smoke", "@week", "@week-label", "@TC-002"],
  31  |   },
  32  |   async ({ page }) => {
  33  |     const home = new HomePage(page);
> 34  |     await expect(home.weekLabel).toHaveText(generateExpectedWeekLabel(0));
      |                                  ^ Error: expect(locator).toHaveText(expected) failed
  35  |   },
  36  | );
  37  | 
  38  | test(
  39  |   "TC-003: Week Label - Previous Week",
  40  |   {
  41  |     tag: ["@week", "@week-label", "@TC-003"],
  42  |   },
  43  |   async ({ page }) => {
  44  |     const home = new HomePage(page);
  45  |     await home.previousWeekButton.click();
  46  |     await expect(home.weekLabel).toHaveText(generateExpectedWeekLabel(-1));
  47  |   },
  48  | );
  49  | 
  50  | test(
  51  |   "TC-004: Week Label - Next Week",
  52  |   {
  53  |     tag: ["@week", "@week-label", "@TC-004"],
  54  |   },
  55  |   async ({ page }) => {
  56  |     const home = new HomePage(page);
  57  |     await home.nextWeekButton.click();
  58  |     await expect(home.weekLabel).toHaveText(generateExpectedWeekLabel(1));
  59  |   },
  60  | );
  61  | 
  62  | /*******************
  63  |       WEEK NAV
  64  | ********************/
  65  | test(
  66  |   "TC-005: Current Week Button - Default State",
  67  |   {
  68  |     tag: ["@week", "@week-nav", "@TC-005"],
  69  |   },
  70  |   async ({ page }) => {
  71  |     const home = new HomePage(page);
  72  |     await expect(home.previousWeekButton).toBeEnabled();
  73  |     await expect(home.currentWeekButton).toBeEnabled({ enabled: false });
  74  |     await expect(home.nextWeekButton).toBeEnabled();
  75  |   },
  76  | );
  77  | 
  78  | test(
  79  |   "TC-006: Previous Week Button - All week navigation buttons enabled",
  80  |   {
  81  |     tag: ["@week", "@week-nav", "@TC-006"],
  82  |   },
  83  |   async ({ page }) => {
  84  |     const home = new HomePage(page);
  85  |     await home.previousWeekButton.click();
  86  |     await expect(home.previousWeekButton).toBeEnabled();
  87  |     await expect(home.currentWeekButton).toBeEnabled();
  88  |     await expect(home.nextWeekButton).toBeEnabled();
  89  |   },
  90  | );
  91  | 
  92  | test(
  93  |   "TC-007: Next Week Button - All week navigation buttons enabled",
  94  |   {
  95  |     tag: ["@week", "@week-nav", "@TC-007"],
  96  |   },
  97  |   async ({ page }) => {
  98  |     const home = new HomePage(page);
  99  |     await home.nextWeekButton.click();
  100 |     await expect(home.previousWeekButton).toBeEnabled();
  101 |     await expect(home.currentWeekButton).toBeEnabled();
  102 |     await expect(home.nextWeekButton).toBeEnabled();
  103 |   },
  104 | );
  105 | 
  106 | test(
  107 |   "TC-008: Current Week Button - from Previous Week",
  108 |   {
  109 |     tag: ["@smoke", "@week", "@week-nav", "@TC-008"],
  110 |   },
  111 |   async ({ page }) => {
  112 |     const home = new HomePage(page);
  113 |     await home.previousWeekButton.click();
  114 |     await home.currentWeekButton.click();
  115 |     await expect(home.weekLabel).toHaveText(generateExpectedWeekLabel(0));
  116 |     await expect(home.currentWeekButton).toBeEnabled({ enabled: false });
  117 |   },
  118 | );
  119 | 
  120 | test(
  121 |   "TC-009: Current Week Button - from Next Week",
  122 |   {
  123 |     tag: ["@week", "@week-nav", "@TC-009"],
  124 |   },
  125 |   async ({ page }) => {
  126 |     const home = new HomePage(page);
  127 |     await home.nextWeekButton.click();
  128 |     await home.currentWeekButton.click();
  129 |     await expect(home.weekLabel).toHaveText(generateExpectedWeekLabel(0));
  130 |     await expect(home.currentWeekButton).toBeEnabled({ enabled: false });
  131 |   },
  132 | );
  133 | 
```